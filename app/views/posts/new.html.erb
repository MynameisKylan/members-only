<h1>New Post</h1>

<%= form_with model: @post do |f| %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "Submit Post" %>
  </div>
<% end %>